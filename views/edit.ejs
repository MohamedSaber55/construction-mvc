<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تعديل البيانات</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 text-gray-900 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-lg rounded-xl p-8 w-full max-w-lg border border-gray-200">
        <h2 class="text-2xl font-extrabold text-blue-700 text-center mb-6">✏️ تعديل البيانات</h2>

        <form action="/update/<%= construction._id %>" method="POST" class="space-y-4">
            <div>
                <label class="block text-gray-700 font-semibold">📍 المكان:</label>
                <input type="text" name="place" value="<%= construction.place %>" required
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <label class="block text-gray-700 font-semibold">🧱 عدد الطوب الأحمر:</label>
                <input type="number" name="redBricks" value="<%= construction.redBricks %>" required
                    class="w-full p-3 border border-gray-300 rounded-lg">
            </div>
            <div>
                <label class="block text-gray-700 font-semibold">🏗️ عدد الطوب المصمط:</label>
                <input type="number" name="solidBricks" value="<%= construction.solidBricks %>" required
                    class="w-full p-3 border border-gray-300 rounded-lg">
            </div>
            <!-- المبلغ المستلم -->
            <div>
                <label class="block text-gray-700 font-semibold">💰 المبلغ المستلم:</label>
                <input type="number" name="receivedAmount" value="<%= construction.receivedAmount %>" required
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500">
            </div>

            <!-- تاريخ الاستلام -->
            <div>
                <label class="block text-gray-700 font-semibold">📅 تاريخ الاستلام:</label>
                <input type="date" name="receivedDate"
                    value="<%= construction.receivedDate ? construction.receivedDate.toISOString().split('T')[0] : '' %>"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500">

            </div>
            <div class="text-center">
                <button type="submit"
                    class="bg-green-600 text-white px-6 py-3 rounded-lg shadow-md hover:bg-green-700 transition w-full font-semibold">
                    💾 حفظ التعديلات
                </button>
            </div>
        </form>
    </div>
</body>

</html>